[
    == pick up as a command ==
    root(P1) isa(P1, pick) modifier(P, Pt) isa(Pt, up) object(P, O) => pick_up!(O);

    mod(E1, A) isa(A, big) => big(E1);
    mod(E1, A) isa(A, red) => red(E1);

    determiner(E1, A) isa(A, an) => an(E1);

    isa(A5, block) => block(E5);
]